<h1>Edit your cocktail </h1>
<%= simple_form_for [:current_user, @cocktail] do |f| %>
  <%= f.input :name %>
  <%= f.input :url, label: "Enter the url of an image of your cocktail!" %>
  <%= f.button :submit, "Update your Restaurant!", class: "btn btn-primary" %>
<% end %>

